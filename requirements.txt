# Yandex Music RecSys contest — runtime dependencies.
#
# For server installs with NVIDIA GPU, install CUDA-enabled torch FIRST,
# then `pip install -r requirements.txt`:
#
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch==2.6.0
#   pip install -r requirements.txt
#
# pip will skip torch on the second call since it's already satisfied.

# Data
polars==1.31.0
pyarrow==20.0.0
pandas==2.2.2          # polars.to_pandas() entry point used by CatBoost.Pool
numpy==2.2.4
scipy==1.15.2          # transitive of implicit (CSR matrices)

# Models
implicit==0.7.2        # ALS, ItemKNN
catboost==1.2.10       # YetiRank ranker
torch==2.5.0           # eSASRec
faiss-cpu==1.13.2      # AudioEmbedKNN HNSW32 (server: faiss-gpu would be faster)

# Optimisation
optuna==4.3.0
scikit-learn==1.6.1    # GroupShuffleSplit for ranker train/val split

# Config / orchestration
hydra-core==1.3.2

# Tests
pytest==8.3.5
